"""Adapters: one module per external system, each implementing a domain Protocol.

yt-dlp, PySceneDetect, Gemini, ffmpeg and the in-memory job store all live here.
Swapping any of them is a one-line change in `dependencies.py`.
"""

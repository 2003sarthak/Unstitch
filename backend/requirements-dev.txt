# =============================================================================
#  Unstitch - development dependencies (never installed in the Docker image)
# =============================================================================
#      pip install -r requirements.txt -r requirements-dev.txt
#
#  Unpinned on purpose: these tools do not ship, so a newer ruff or pytest is a
#  free improvement rather than a deployment risk.
# =============================================================================

pytest>=8.0
pytest-asyncio>=0.24
# Required by fastapi.testclient / httpx.ASGITransport for API contract tests.
httpx>=0.27
ruff>=0.6
